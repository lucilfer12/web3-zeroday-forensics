# Corpus

`cases.yaml` is the compact index of curated cases. Detailed records live in `cases/`.

`upstream/` is reserved for generated snapshots from public incident indexes. Upstream entries are leads until they pass this project's source and timeline checks.

The corpus is deliberately multi-status. Not every exploited vulnerability is a zero-day, and not every public research record has enough evidence to establish a zero-day timeline.
